import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },
  loadingContainer: {
    padding: 20,
  },
  fab: {
    position: "absolute",
    right: 16,
    bottom: 16,
  },
});
